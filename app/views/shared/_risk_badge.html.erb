<%# Renders project risk level badge (matches Risk view table). Local: project %>
<% level = project.risk_level %>
<span class="risk-level-badge risk-level-badge--<%= level %>" title="Project risk"><%= level.to_s.capitalize %></span>
